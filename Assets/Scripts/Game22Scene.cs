using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Game22Scene : GameManager
{
    // Start is called before the first frame update
    void Start()
    {
        player1_hap = 100;
        player2_hap = 100;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
